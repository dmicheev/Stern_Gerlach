{
  "name": "stern-gerlach",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "dev": "node scripts/run.mjs dev",
    "start": "node scripts/run.mjs dev",
    "build": "node scripts/run.mjs build",
    "test": "pnpm --dir frontend test && cargo test --manifest-path physics-wasm/Cargo.toml",
    "typecheck": "pnpm --dir frontend typecheck",
    "build:wasm": "wasm-pack build --release --target web physics-wasm --out-dir ../frontend/src/physics/wasm-pkg"
  }
}
